[package]
name = "BL"
version = "1.0.0"
edition = "2021"
resolver = "2"
# default-run = "BL"

[features]
default = ["download"]
download = ["curl"]
curl = []
wget = []
batch = []
retry = []

[[example]]
name = "color"
path = "src/color.rs"

[dependencies]
crabquery = { version = "*", default-features = false }
serde_json = { version = "*", default-features = false, features = ["std"] }


