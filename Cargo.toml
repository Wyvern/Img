[package]
name = "img"
version = "1.0.0"
edition = "2024"

[lints.rust]
linker_messages = "allow"

[[example]]
name = "color"
path = "src/color.rs"
test = false
# required-features = ["color"]

[dependencies]
alphanumeric-sort = "*"
argh = "*"
base64 = { version = "*", default-features = false }
cbor4ii = { version = "*", features = ["serde1", "use_std"] }
dom_query = { version = "*", default-features = false }
fastrand = "*"
percent-encoding = "*"
serde_json = "*"

[dev-dependencies]
# bincode-next = "*"

[build-dependencies]
cbor4ii = { version = "*", features = ["serde1", "use_std"] }
serde_json = "*"

[target.'cfg(target_os = "hermit")'.dependencies]
hermit = { git = "https://github.com/hermit-os/hermit-rs.git", branch = "main" }

[target.'cfg(target_os = "espidf")'.dependencies]
# esp-idf-sys = "*"

[target.'cfg(all(unix, not(target_os = "espidf")))'.build-dependencies]
libc = "*"

[target.'cfg(all(unix, not(target_os = "espidf")))'.dependencies]
file-format = "*"
libc = "*"

[features]
default = []
color = []
download = []
